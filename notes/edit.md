# 編集と変形

### 4.9 消去（25 番）—— **3 回の押しが通ります**

`src/cmd.c` と `src/ui.c`。流れは

1. 左で始点 —— ポインタまで**色 2 の排他的論理和の矩形**がついてくる
   （□ と同じ。白い線を跨ぐと 00ffff になるので 7 XOR 2 だと確かめました）
2. **右**で終点 —— 範囲確定。`jw_cmd_marked()` が箱に**まるごと入った**
   線・円弧・文字・点を色 2 で描き直します。本物と**作図領域 0 画素差**
   （`sh tools/pressfull.sh 25 150 130 r 245 170` が **0 画素差**）
3. **上の行の `①実行`** —— 消える。`./tests/drawing.exe -o -u -c 25
   -p 150 130 -r 245 170 -t 186 -m 600 450 orig/SAMPLE0.JWC out.raw` を
   `GO_X=186 sh tools/erase.sh 150 130 245 170` の `tmp/erase/a.raw` と
   比べて、**作図領域は 2 画素差**。その 2 画素は本物が消すときに
   隣の線の端を巻き込んで塗り残したもので、線消と同じ種類です

**上の行はメニューです**（`jw_ui_top_item()`）。`|` で区切られた区間が
項目で、左から 1、2、…。最初の `|` より前と、`|` そのものの桁は 0。
本物で 5 点押して確かめました（24〜33 桁が実行、34 桁目は無反応、
35〜44 桁が中止）。`jw_ui_text()` が 1 行目を書くたびに 80 桁ぶんの
控えを取り、`jw_ui_draw()` が 1 行目を黒で塗るところで消しています。

**まだのところ**:

* ~~実行したあとの上の行~~ —— いまは合っています。
  `./tests/drawing.exe -o -u -c 25 -p 150 130 -r 245 170 -t 186 -m 600 450`
  と `GO_X=186 sh tools/erase.sh 150 130 245 170` の差は **2 画素**で、
  どちらも本物が消すときに隣の線の端を巻き込んで塗り残したものです。
* **2 回目を左で押したときの追加･除外が入りました**（4.9 の続き、下）。
* 上の行の `|①範囲内消去|` などの切り替え、`[F1]`〜`[F10]` の絞り込みは
  まだです。
* 円弧が「まるごと入っている」かは中心と半径の箱で見ています。線と文字は
  両端で決まると測れていますが、円弧はそれを分ける図面が 14 枚に無く、
  箱で置いてあります（`arc_in_range()` のコメント）
* ~~`src/main_wasm.c` にはまだ上の行の押しの入口がありません~~ ——
  入りました。`jw_click` は y が 0〜15 で `jw_ui_top_item()` が項目を返せば
  `jw_cmd_top()` に渡します

**追加･除外（2 回目を左で押したとき）**。`sh tools/erase2.sh 150 130 245 170
197 157` が本物と移植を同じ並びに通して比べます（SAMPLE0 で 2 画素、上と
同じ塗り残し）。

* 右で押したときと**同じ範囲が選ばれます** ——SAMPLE0 の
  (150,130)-(245,170) でどちらも赤が 224 画素。違うのは上の行だけで、
  `消 去  追加･除外データ指示   線・円(L) 文字(R)  [F1～F10] |①範囲 確定|`
  になります（`src/typed.h` の 25 番 3 段目）。
* そこで**線を押すと選択から外れます**（赤→白 70 画素）。もう一度押せば
  戻ります。選択は「範囲の中 ⊕ 手で裏返した並び」で持っています
  （`src/cmd.h` の `flip`）。
* **範囲の箱がこの段だけ画面に残ります** ——色 4 の**排他的論理和**で 4 辺。
  平らに塗っているのではないと分かるのは、**四隅が黒い**（各隅が 2 回引かれて
  戻る）のと、青い画素と重なったところが緑ではなく赤紫（1 ⊕ 4 = 5）になる
  ためです。右で押したときの画面には緑が 1 画素もありません。
* `①範囲 確定` で `復活出来ません |① 実行(L)|② 中止(R)|` に進み、
  `①実行` で外したものを除いて消えます（SAMPLE0 で 30|13 → 29|12）。
* **文字(R) も入りました。** 拾うのは「基線を x にも y にも 10 だけ広げた
  箱の中に点があるか」——距離ではありません。SAMPLE0 の文字 0
  （(51.17,310.96)-(93.03,310.96)）で測ると、12.88 離れた点も 14.09 離れた
  点も拾い、10.96 の点は拾わない。x で 9.97 は入り 10.97 は入らず、y で
  9.04 は入り 10.04 は入らないので、境目は 10 です（線の 8 より大きい）。
  `jw_cmd_text_at()`。傾いた文字と、重なった文字のどちらが勝つかと、
  文字種でも絞るのかは測っていません。
* **SAMPLE6 で合わなかったのは解けました。** 下の「4.9b 追加･除外が拾うもの」。



範囲を箱で囲んで消す命令です。本物を動かして流れを全部読みました。
道具は `sh tools/erase.sh 150 130 245 170`（`DRAWING=` と `WAIT=` が効きます）
——3 回の押しを本物にさせて、色の変わりかたと上の行を出します。
メニューの位置は左の列の 10 行目、`mouse 30 216`。

**押しは 3 回です。**

| | 押す | 本物がすること |
|---|---|---|
| 1 | 左（始点） | 上の行が `線･円･文字 終点指示 ﾏｳｽ(L)  範囲確定 ﾏｳｽ(R) \|①レイヤ\|②線種色\|③文字種\|` になる |
| 2 | 右（終点＝範囲確定） | 箱に**まるごと入っている**実体が**色 2（赤）で上から描き直される**。上の行が `復活出来ません \|① 実行(L)\|② 中止(R)\|` になる |
| 3 | **上の行の `①実行` を左**（(220,8)） | 選ばれた実体が消える（赤だった 224 画素が黒に）。本数の隣（24 桁目・2 行目）に `消去 再度(L)` が出て、上の行はそのまま |

**3 回目は作図領域ではなく上の行を押します。** `(L)` と書いてあっても
作図領域の左ボタンでは実行されません——2 回目のあとに作図領域を押しても
赤いままで、`消去 再度(L)` が出るだけです。`①実行` の桁は 24〜33
（`復活出来ません ` が 8 桁目から 15 桁ぶん、`|` が 23 桁目）。

測った中身:

* **まるごと入っているものだけ**が選ばれます。SAMPLE0 で (150,130)-(245,170)
  を囲むと、線 5（162,157)-(231.7,157)と線 6 と文字 0 は消え、箱を横切る
  だけの線 1（y=139.94, x162..598）は消えません
* 選択の赤は `JW_PAL.DAT` の 2 番（f30000）。囲んだ矩形は 4 番の緑
  （00ff00）で、(250,150)-(450,350) の周 800 画素のうち 786 が塗られます
  ——**これは 2 回目の押しのあとの画面で測ったもの**で、1 回目のあとに
  ポインタについて回る段の色と rop（排他的論理和かどうか）は未測定です
* **読取可能なレイヤだけ**です。SAMPLE6（書込レイヤ 2）でレイヤ 0 の
  線 38〜41 を囲むと 440 画素が赤になり、TEST7 の「表示のみ」レイヤ
  0c/0d/0e だけが入る箱 (440,280)-(480,320) では**赤が 1 画素も出ません**
  （TEST7 は読み込みが遅いので台本の頭の待ちを 7000 万命令にすること。
  4000 万だとメニューを押す前に間に合わず、別の項目が選ばれます）
* 箱に何も入っていないと、`①実行` を押しても何も消えず `消去 再度(L)` だけ出ます
  （SAMPLE0 で (250,150)-(450,350) は枠を横切るだけなので 1 本も消えない）
* **下の帯は変わりません**。選択中に本物は下の帯の文字を書き直しますが、
  中身は同じで、画面は 1 画素も変わりません（差は上の行と作図領域だけ）

**書くときの見通し**（`src/cmd.c` / `src/view.c` を足す）:

1. `src/stage.h` に 25 番を足す —— `tools/stage_table.py` の `COMMANDS` に
   `(25, (150, 130, 'r', 245, 170, 'l', 245, 170))` のような行を入れて回す。
   ただし `tools/press.sh` の `r`/`l` 印はもう通ります
2. `JwCmd` に箱の 2 点目（`x1,y1`）と段（`pressed` 0/1/2）を持つ
3. `jw_cmd_band()` に矩形を足す。落ち着いた矩形は色 4 だと測れています。
   □ のゴムひもは色 2 の排他的論理和なので別扱いです。**引いている途中の
   色と rop は測っていません** —— 1 回目を押してからポインタを動かした
   画面を 2 枚撮って比べるところから始めてください
4. 選択の赤は「図面を描いたあとに、選ばれた実体だけ色 2 で描き直す」。
   本物もそうしています（白→赤に変わった画素が 224、ほかは動かない）
5. 3 回目は**上の行の押し**なので、上の行を押せるようにするのが先です
   （4.10 参照）。そこで `jwc_remove_line` / `jwc_remove_arc` と、文字・点の
   同じものを足して消す
6. 検査は `tools/pressfull.sh` が作図領域の押ししか渡せないので、
   上の行を押す道具（`tools/erase.sh` の形）を足すことになります

**まだ測っていないこと**: `\|③指定範囲\|`（複写・移動と同じデータ選択）。
②範囲外消去 は 4.9c、文字(R) は上、`[F1]`〜`[F10]` は 4.9b の終わり。

### 4.9b 追加･除外が拾うもの —— **解けました**（2026-09-18 に一度書き直しました）

**追加･除外 は何でも拾います。**書込みのペンと線種で絞るのは
**修飾キーを押したときだけ**です。

長いあいだ「本物は実体を組でまとめて持っているらしい」と書いてありました。
**それも外れでした。**（そう見えたのは、エミュレータの 64KB バグで本物の
メモリ上の実体配列が途中から化けていたからです——上の「1.」を参照。）

本当の規則:

> 追加･除外 も 線消 も、**届く範囲にある実体なら何でも**拾います。
> **修飾キーを押しながら押した**ときだけ、いま書込みに選ばれている
> 線種とペンで描かれた実体に絞られます。

> **2026-09-18 まで、ここには「ペンと線種に合うものしか拾わない」と
> 書いてありました。**それはエミュレータが `INT 16h AH=12h` に答えず、
> 本物が「Ctrl が押されている」と思い込んでいたからです（「刺された罠」）。
> `mods ctrl` を入れれば当時の答えがそのまま出ます。

当てはめではなく、本物に訊いて出しました。道具は 2 つ:

```sh
python tools/mkpick.py orig/QBYTES.JWC bytes   # 質問用の図面を作る
sh tools/pickat.sh 300 60 300 90 ...           # 本物が何番を答えたか読む
sh tools/pickcheck.sh                          # 本物と移植を並べる
```

`tools/pickat.sh` は消去で範囲を囲んでから点を押させ、探索
`11f2:573f` の**入口**（渡された点が double 2 つでスタックに載っています）と
**直後の格納** `11f2:56a2`（`mov [bp-1ah],ax`、DX:AX が答え）の両方で止めて、
対にして出します。**答えは実体の番号（1 から数えた）**です——SAMPLE0 の
線 5 を押せば 6、SAMPLE6 の線 779 を押せば 780。0 が「読取可能データ無」。

両方で止めるのには理由があります。**範囲のあとの最初の押しは探索まで
届きません**（n 回押すと n−1 回しか呼ばれず、落ちるのはいつも最初）。
数えて対応づけると全部ずれます。`DUMMY` で捨て駒を 1 回押しています。

`tools/mkpick.py ... bytes` は、座標のうしろのバイトだけが違う線を 12 本、
10 画素おきに置きます。押してみると **12 本とも拾えます**。
スクリプトに `mods ctrl` を入れて同じことをすると、拾えない 2 本が出ます——
「線種 2」のものと「ペン 5」のもの（SAMPLE0 は線種 1・ペン 2 で書いています）。
余りの 4 バイトは何をしても関係なく、レイヤも（表を全部立てておけば）
関係ありません。逆アセンブルにも同じ判定があります（`11f2:5993` あたり、
`ペン != DGROUP 0xa6a || 線種 != DGROUP 0xa6c` なら飛ばす。この 2 つは
`src/jwc.h` にある盤面のペンと線種）。その手前で修飾キーの状態を
`1885:5307` から読んでいて、それが判定を開け閉てします。

SAMPLE6 の壁（ペン 1・2・5）も**外せます**。「ペン 4 で書いているから
壁が拾えない」と長く書いてありましたが、拒否していたのは幻の Ctrl でした。

ついでにもう一つ測れました。**同じ距離なら先のレコードが勝ちます**
（移植は `<=` で後ろを取っていました）。届く範囲は 8 未満——7 は拾い、
8 は拾いません。

合わせた結果:

| | |
|---|---|
| `sh tools/pickcheck.sh`（質問図面 13 点） | 全部一致 |
| SAMPLE0 の 5 点 | 全部一致 |
| SAMPLE6 の 44 点（線の中点を散らして） | 全部一致 |
| `DRAWING=SAMPLE6 ... sh tools/erase2.sh 170 235 215 260 209 242` | **170 画素 → 1 画素** |

実行まで通すと 15 画素残りますが、**これは前からある本物の塗り残し**
です。15 画素はどれも「本物は黒・移植は描いている」で、散らばりかたも
1 画素・2 画素の飛び飛び——消した実体のまわりを本物が塗りつぶすときに
隣の線や円の端を巻き込み、そのまま描き直さないという、線消 のときと
同じもの（SAMPLE0 で 2 画素、SAMPLE6 の円弧で 4 画素）。密なところを
消したので数が増えているだけです。確定前は **1 画素差**です。

**[F1]〜[F10] も測りました。効くのは [F2] だけで、選択がまるごと消えます。**

SAMPLE0 の `(150,130)-(245,170)` で [F2] を押すと、赤かった 224 画素が
それぞれの色に戻り、そのまま `①範囲 確定` → `①実行` まで通しても
**1 本も消えません**（本数は 30|13 のまま）。SAMPLE6 でも同じ（182 画素）。
もう一度押しても戻らず、[F1] と [F3]〜[F10] はいつ押しても何も起きません。

**追加･除外 の最初の押しは `11f2:573f` まで届きません。** n 回押すと n−1 回
しか呼ばれず、落ちるのはいつも最初の 1 回。別の道を通っているらしく、
左ボタンならその押し自体は効きます（`tools/erase2.sh` の SAMPLE0 は捨て駒
無しで合う）が、**右ボタンの 文字(R) は最初の押しでは効きません**。
`tools/pickat.sh` と `tools/erase2.sh` の `DUMMY=` はそのための捨て駒です。

**そのあとの押しは「足す」ほうになります。** [F2] のあと (197,157) を
押すと白い 70 画素が赤くなる。つまり範囲の答えを捨てて、押した実体だけで
選び直す。移植では `JwCmd.cleared` が立ち、`jw_cmd_in_range()` が 0 を返し、
反転の並びは空にします。

`JW_VER.DOC` に「`-L6` オプション設定で、データ選択の |1)範囲確定|のとき、
ファンクションキー（[F2]〜[F10]）を続けて２回押すと、指定した属性のみの
データを選択することができるようにした」とあります。**このオプション無しで
起動しているので、属性の指定は効きません**——[F2] の「消す」ほうだけが残る。

    FKEY=2 sh tools/erase2.sh 150 130 245 170 197 157         # 2 画素（本物の塗り残し）
    FKEY=2 STOP=1 sh tools/erase2.sh 150 130 245 170 197 157  # 0 画素

### 4.9c ②範囲外消去 —— **切り取り**です（`src/cmd.c` の `outside_kind`）

消去を選んだ直後の上の行は
`●消去範囲 始点指示 |①範囲内消去|②範囲外消去|③指定範囲|` で、桁は
①が 29〜40（x 224〜327）、②が 42〜53（328〜431）、③が 55〜64（432〜519）。

`JW_VER.DOC` に「消去コマンドのうち、範囲内／範囲外消去を、切り取り消去と
した」とあり、本物もそのとおりでした。SAMPLE0 を範囲 (150,130)-(245,170) で:

| | |
|---|---|
| 箱の**外**にまるごとある線 | 選ばれて消える（11 本中 7 本：2,3,4,7,8,9,10） |
| 箱の**中**にまるごとある線 | 選ばれない（5 と 6） |
| 箱の縁を**またぐ**線 | **縁で切られ、中に残っていたほうが残る**（線 1 は x 40.97〜477 が 40.97〜124 になって戻ってくる） |

本数は 30|13 → 23|10。またいだ線はレコードとしては 1 本のまま短くなります。

**画面もそう言っています。** ①実行 を押す前、まるごと外の線は**べた赤**、
またぐ線は**点線の赤**——1 画素おき、`jw_view_line_style(0)` の 0x5555 です。
しかも**下に白い線が透けません**：隙間は黒。本物は線をいったん黒で塗って
から点線の赤を乗せています（隙間が 000000 で、もとの ffffff ではない）。

**追加･除外 はこの段でも効きます。**

* 中にある線を押すと**赤くなる**（SAMPLE0 の線 5 で 69 画素）
* またぐ線を押すと**点線がやめになり白に戻る**（線 1 の 437 画素が戻る）
  ——つまり切り取りから外れる

**上の行に `＿` が出ません。** 範囲内消去 の 追加･除外 は 6 桁目に `＿`
（左ボタンのドラッグが使える印）を書きますが、範囲外消去 では書きません。
16 画素の差になるので `JwUi.outside` で分けてあります。

道具は `OUT=1 sh tools/erase2.sh`。

```sh
OUT=1 DUMMY="600 450" STOP=1 sh tools/erase2.sh 150 130 245 170 600 460   # 0 画素
OUT=1 DUMMY="600 450" STOP=1 sh tools/erase2.sh 150 130 245 170 197 157   # 0 画素
OUT=1 DUMMY="600 450" sh tools/erase2.sh 150 130 245 170 197 157          # 1 画素
```

またぐ線を押した場合だけ 2 画素残りますが、どれも**本物が押した線を
いちばん上に描き直す**ぶんで、交点の色が違うだけ（4.6 と同じ種類）。

**測っていないこと**: 円弧と文字が縁をまたいだとき。SAMPLE0 の見える文字は
どれも中か外にまるごとあり、円弧は 1 つもありません。移植は「まるごと外に
あるものだけ選ぶ」ことにして、またいでいるものには手を出しません
（`wholly_outside()`）。`③指定範囲` は 4.9d にあります。

**ついでに直したこと**: ①実行 のあと `読取可能データ無` が残っていました。
本物はそこに `消去 再度(L)` を書くので、実行で消すようにしました。

### 4.9d ③指定範囲 —— **できました**（`src/span.h`、`src/cmd.c` の `span`）

消去の 3 つ目の枝で、**複写・移動と同じデータ選択**です。上の行の
③（桁 55〜64、x 432〜519）を押すと、消去の行が丸ごと入れ替わります:

```
指定範囲  始点マウス指示  (L)線･円  (R)線･円･文字   [F1～F10] |① 前 範 囲|
```

**1 点目のボタンが「何を網にかけるか」を決めます。** SAMPLE0 を
(150,130)-(245,170) で囲むと

| 1 点目 | 赤くなる画素 | 中身 |
|---|---|---|
| 左 `(L)線･円` | 89 | 線 5 と線 6 だけ |
| 右 `(R)線･円･文字` | 224 | それに文字 0 を足したもの |

上の行もそれに合わせて 2 通りあり、左だと 8 桁目が `<線･円>`、右だと
`線･円･文字` で、右のときだけ `③文字種` が増えます（`src/span.h` は左のほうを
**段 11** として持っていて、`src/ui.c` が `JwUi.with_text` で選びます）。

2 点目（左）で範囲が決まると、あとは 追加･除外 と同じです——
`指定範囲  追加･除外データ指示   線・円(L) 文字(R)  [F1～F10] |①範囲 確定|`、
`①範囲 確定` で `復活出来ません |① 実行(L)|② 中止(R)|`（この行だけは
①範囲内消去 と同じものを使い回します）、`①実行` で消えます。

**①実行 のあと 指定範囲 は解けます。** 本物は最後に `◇消去範囲 始点指示
|①範囲内消去|②範囲外消去|③指定範囲|` を書き戻すので、次の範囲は
①範囲内消去 に戻ります。

**範囲の緑の枠は「ポインタが動いてから」ではありません。** 2 点目を押した
その場で 4 本出ます（色 4、排他的論理和）。移植は `moved` を待ってから
ゴムひもを描く作りだったので、枠だけその判定の**前**に移しました——
`STOP=1 sh tools/span.sh 150 130 245 170` は 2 点目の点にポインタを置いた
まま撮るので、これで 264 画素の差が消えます。

検査は `sh tools/span.sh`（`STOP=1` で確定前、`R1=1` で 1 点目を右、
5・6 番目の引数で 追加･除外 の点）。**画面全体 0 画素差**、通しで 2 画素、
追加･除外 を 1 回挟むと 1 画素——どれも本物が押した線を上から描き直す
ぶんです（4.6 と同じ種類）。

**まだのところ**: `|① 前 範 囲|`（前回の範囲を呼び出す）、`[F1]〜[F10]`、
`|①レイヤ|②線種色|③文字種|`（2 点目を押す前の絞り込み）。

### 4.9e 複写（1 番）—— **範囲選びは通ります。置く押しが謎です**

メニューは右の列の 1 行目（`mouse 90 72`）。上の行は `src/copy.h`
（`tools/copy_table.py` が本物から起こします）。検査は `sh tools/copy.sh`
（`STEP=` で止まる段、`R1=1` で 1 点目を右、`MOVE="x y"` で最後にポインタを
動かす）。**段 2・3・4 は左右どちらの 1 点目でも画面全体 0 画素差**です。

| 段 | 押す | 本物 |
|---|---|---|
| 1 | 範囲の始点（左＝線･円 / 右＝線･円･文字） | `線･円･文字 終点指示 ﾏｳｽ(L)  範囲確定 ﾏｳｽ(R) \|①レイヤ\|②線種色\|③文字種\|`——**③指定範囲 の段 1 と 1 桁も違いません**（左だと `<線･円>` で ③文字種 が無い） |
| 3 | 範囲の終点（左） | `複写範囲  追加･除外データ指示   線・円(L) 文字(R)  [F1～F10] \|①範囲 確定\|`、6 桁目に `＿` |
| 4 | 上の行の `①範囲 確定`（桁 69〜78） | `\|①ﾏｳｽ位置(L,R)\|②数値位置\|③数値倍率\|④ﾏｳｽ倍率\|⑤反転\|⑥回転\|⑦属性変更\|`、2 行目 72 桁に `変更無し`（色 7・塗りつぶし） |
| 5 | 上の行の `①ﾏｳｽ位置`（桁 9〜22） | `複写  原図形の基準点位置 マウス指示 (L)free (R)Read  \|①【任意】方向\|` |
| 6 | 作図領域（基準点） | `複写 位置 マウス指示 (L)free (R)Read  \|①【任意】方向\|②仮表示\|③回転角\|` |

**どれも【】が付いていないうちは選ばれていません。** 段 4 の 7 つは
どれも括弧無しで、`①ﾏｳｽ位置` を押して初めて基準点を聞きに来ます
（押さずに作図領域を押しても基準点は取れますが、上の行は出ません）。

**読取の言葉（17 桁目・22 桁目）は、修飾キーを押している間だけです。**
何も押していなければ複写のどの段でも帯は真っ黒です。[CTRL] を押しながら
だと、範囲を取っている間（段 1〜4）は出ず、`①ﾏｳｽ位置` のあと段 5 から
22 桁目に `円周1/4点` が出ます。`src/ui.c` はこれを `JwUi.ctrl` で塞いで
います（4.8 と「刺された罠」）。

**段 6 の画面は突き合わせられません。** 本物は選んだ実体を色 2 で塗った
うえに、**複写の下絵を排他的論理和で重ねて**います。撮った瞬間によって
出ていたり消えていたりし、文字まで範囲に入れると同じ 1 枚に
赤・紫・青・緑・水色が混ざります（2 xor 4 = 6 で黄色に見えるので、
一度「段 6 は色 6」と読み違えました）。`tools/copy.sh` の `STEP=5` は
そのぶん差が出ます。

**①ﾏｳｽ位置 は入りました。** 段 4 の `①ﾏｳｽ位置(L,R)`（桁 9〜22、x 64〜175）を
押すと段 5（`原図形の基準点位置`）、そこで押すと段 6（`位置`）、そこで押すと
**複製ができて段 9** になります:

```
[ESC]・再複写 位置指示(L)free (R)Read |①同形別処理|②他図形処理|③連続|
```

（移動は `再移動 位置指示`。`src/copy.h` / `src/move.h` の段 9。）
**そこで終わりではありません**——段 9 でもう一度押すと**もう 1 つ置けます**。
SAMPLE0 で 30|13 → 32|14 → 34|15。`sh tools/copypos.sh` と
`MORE="300 300"`・`AGAIN=1`・`THEN=350` で検査できて **0 画素差**です。

測ったこと:

* **どちらの押しも `(L)free (R)Read`**。ふつうの点取りと同じで、左は
  ポインタそのまま、右は読取
* **複写は原図形からの距離**です。基準点は動かないので、段 9 でもう一度
  押すと新しい距離にもう 1 つ増えます
* **移動は元の位置からの絶対位置**です。基準点が押した所へ来るように動くので、
  2 回目の押しでは「前の行き先からの差」だけ動きます。移植では
  `place_at()` が移動のときだけ基準点を押した点に置き換えています
  （SAMPLE6 ではなく SAMPLE0 の (200,150)→(383,401)→(300,300) で確かめました。
  本物は最後に元位置 +(100,150) にいます）
* **範囲が何を掴んでいるかは最初の 1 回だけ数えます。** 移動は実体を連れて
  行ってしまうので、2 回目に箱を数え直すと中に何も残っていません
  （`place_by()` の `if (!c->sel_line)`）
* **②数値位置 が覚えている距離は変わりません。** マウスで複写したあと
  ①同形別処理 → ②数値位置 と進んでも `[  1000.000,  1000.000 mm]` のままです。
  だから `Jwc.copy_x_mm` には触りません。③連続 が繰り返すぶんは
  `JwCmd.step_x/step_y`（作図単位）に入れています
* 段 9 の `①同形別処理`（桁 40〜51、x 312〜407）・`②他図形処理`（桁 53〜64、
  x 416〜511）・`③連続`（桁 66〜71、x 520〜567）は段 8 と同じ働きです
* 移動で残る 2〜26 画素は**本物の側**です。動いたあとを消すときに隣の線を
  巻き込んで描き直さないので（4.9f と同じ）

**なぜ 1 年ぶん詰まっていたか**——エミュレータが `INT 16h AH=12h`
（拡張シフト状態）に答えていなかったからです。JW_CAD は押しごとにこれを読み、
返ってきた語の下位バイトを `and ax,0eh` で {2=Shift, 4=Ctrl, 8=Alt} に
落として `[0c1e]` に入れます。答えないと `int86()` の呼び出し側に残っていた
値がそのまま帰り、それは `0x94`——**Ctrl のビットが立っていました**。
`[0c1e] == 2` だと点取り `11f2:34e9` は「修飾つきの読取」の枝に入り、
そこは命令側が `[0ce6]` を立てていない限り `11f2:365a` の
「読取可能データ無」へ跳びます。押し方の問題では全くありませんでした。
辿った道は `11f2:38a8  cmp [0c1e],2 / cmp [0ce6],0 / jmp 365a`。
直したのは dosv_emu_cpp 側（そちらの RESUME の「刺された罠」）。

**②数値位置 は入りました。** 段 4 で `②数値位置`（桁 24〜33、x 184〜263）を
押すと

```
[ESC].距離 X,Y =        前回と同じ ﾏｳｽ(R)  [  1000.000,  1000.000 mm]
```

になり（段 7）、18 桁目から 1 文字ずつ打ち込んで [Enter] で複製されます。
作図領域を**右**で押せば 前回と同じ。どちらも終わると段 8 の
`|①同形別処理|②他図形処理|③連続|` になります。**画面全体 0 画素差**
（`sh tools/copynum.sh 20,30`、`5`、`SAME=1`、`STOP=1`）。

測ったこと:

* **距離は紙のミリ**で、複線と同じ換算（`mm * unit_mm / denom`）。
  SAMPLE0（S=1/1）で `20,30` は右に 35 画素・上に 52 画素——
  20×1.744 と 30×1.744 を切り捨てたもの
* **数がひとつだけなら縦横とも同じ**（`2` だけ打つと両方に 2mm）
* **打ち込みは 1 文字ずつ送ること**。まとめて送ると本物が読みきれず
  最初の 1 文字しか届きません（`tools/copynum.sh` がそうしています）
* 最後に使った距離は覚えていて、次の `[…]` に出ます（`Jwc.copy_x_mm`）。
  起動時は 1000.000
* **複製は選択に入りません。** 5mm だけずらすと複製が範囲の箱に入って
  しまいますが、本物は複製を赤くしません。範囲を決めた時点の本数を
  覚えておいて、それより後ろは選択から外します（`JwCmd.n0_lines` ほか）
* **複製は赤の上に描かれます。** 重なったところは複製の白が勝ちます——
  本物は新しい実体を出来上がった画面に上から描くので（4.6 と同じ）。
  移植も、印を付けたあとに新しいぶんだけ描き直します

**①同形別処理 と ②他図形処理 も入りました。** できあがったあとの
`|①同形別処理|②他図形処理|③連続|` で、① は**同じ選択のまま**
`|①ﾏｳｽ位置(L,R)|…|`（段 4）へ戻り、② は**選択を捨てて**その命令が出て
きたときの行（段 0）へ戻ります。桁は ① が 9〜20（x 64〜167）、
② が 22〜33（168〜271）、③ が 35〜40（272〜327）。
`THEN=120`/`THEN=220` で検査できます。

**[CTRL] を押しているときの読取の言葉（22 桁目の 円周1/4点）は段 4・5・6
だけです。** 範囲を取っている間（段 1〜3）も、距離を聞いている間（段 7）も、
できあがったあと（段 8）も出ません。①同形別処理 で段 4 に戻ると出ます。
何も押していなければどの段でも出ません。

**③連続 は入りました。** 複製できたあとの `|①同形別処理|②他図形処理|③連続|`
（③ は桁 35〜40、x 272〜327）を押すと、**同じ選択がもう一段先に**もう一度
複製されます——SAMPLE0 で 20,30 なら 1 つ目が右 35・上 52、2 つ目が右 70・
上 104。本数は 32|14 → 34|15 → 36|16。`AGAIN=n sh tools/copynum.sh` で
検査できて **0 画素差**です。

**選んだものの赤も、新しく作ったものも、盤面を描いたあとに描きます**
（`jw_cmd_marked()` と `jw_cmd_after()` を `jw_ui_draw()` のあとで呼ぶ）。
本物は新しい実体を出来上がった画面に上から描くので、移植も同じ順にしないと
合いません——③連続 の 2 つ目は上の帯（y35〜52）に届き、移植が毎回やっている
「項目を選んだときの (122,17)-(638,47) の黒塗り」に 105 画素消されていました。
切り取りも図面と同じにする必要があるので、`jw_view_line()` を view.c から
切り出しています（`jw_line` は切り取りません。図面の側が `inside()` と
`clip_far()` でやっています）。盤面は切り取り枠を画面全体に開けて帰るので、
どちらも描く前に図面の窓へ戻します。

移動の ③連続 も同じ理由でした——動いた実体が帯（y35〜52）に入り、
赤い印が黒塗りに 105 画素消えていました。移動の 連続 は**もう一段動かす**
（複製ではないので本数は変わらず、2 回で 2 倍の距離）。

複写で残っているのは ⑤反転・⑥回転・③数値倍率・④ﾏｳｽ倍率・⑦属性変更 です。

### 4.9f 移動（16 番）—— **複写と同じで、写さずに動かします**

メニューは左の列の 1 行目（`mouse 30 72`）。範囲の取りかたも、
`①範囲 確定` も、`①ﾏｳｽ位置`/`②数値位置` も複写と同じで、上の行は
言葉が 2 つ違うだけです（`移動範囲`、`⑦ﾚｲﾔ移動`。複写の 2 行目にある
`変更無し` は出ません）。`src/move.h` は `tools/copy_table.py` が
複写と同じやりかたで起こします。

`②数値位置` で距離を打つと、選んだものが**そのまま動きます**——本数は
30|13 のまま。`sh tools/copynum.sh` に `CMD=16` を付ければ検査できます。
段 2・3・4 と打ち込み中は**画面全体 0 画素差**、動かしたあとは 2 画素で、
これは本物が消した跡の隣の線を塗り残すぶんです（4.6 と同じ種類）。

**選んだものは箱ではなく覚えておきます。** 移動すると実体が箱から出て
しまうので、箱で judge し直すと印が消えます。本物は消えないので、
複写・移動が実際に動く直前に「何が選ばれていたか」を 1 バイトずつ
書き留めます（`JwCmd.sel_line` ほか、`freeze()`）。`JwCmd` は最初の
`jw_cmd_pick` の前に 0 にしておくこと——`pick` が前のぶんを解放します。

**罠**: `DOSEMU_WATCH` の当たりは**標準出力**に出ます。`2>&1 >/dev/null` に
すると「watching …」（標準エラー）だけ残って当たりが消え、**「誰も書いて
いない」という嘘の結論**が出ます。3 回やりました。

**上の行の桁**（`複写 位置 …`）: 縦棒は 46・61・70・79 桁。
`①【任意】方向` は 47〜60（x 368〜487）で、押すと
【任意】→【Ｘ軸】→【Ｙ軸】と回ります。`②仮表示` は 62〜69、
`③回転角` は 71〜78。

**移動（16 番）は未着手**ですが、上の行の始まりは複写と同じ形です。

### 4.15 線変更（24 番）—— **できました**（`src/cmd.c`）

メニューは左の列の 9 行目、`mouse 30 200`。**押しは 1 回**、指した線か円弧が
書込みのペン・線種・レイヤになります。上の行は最初から最後まで

    ・１本線･円変更 マウス指示 |①指定範囲内変更|②レイヤ変更【有】|③属性設定|

のままで、変わるのは**本数の隣**（2 行目の 20 桁目と 22 桁目）に出る
`線 変更` / `円 変更` だけ。

**中身は本物に保存させて読みました**（`PRE=` 付きの `tools/save.sh`）。
SAMPLE6 はペン 4・線種 1・レイヤ 2 で書いていて、その線 38（ペン 1、
レイヤ 0）を押すと

    前  type=1 pen=1 rest=00 f6 00 08
    後  type=1 pen=4 rest=02 f6 01 08

——ペンとレイヤと、**余り 4 バイトの 3 つ目のビット 0**。これは**実体
探索が見つけたものに付ける印**です（4.9b：走査は全レコードで落として、
答えたものにだけ立てる）。ほかは 1,189 本のうち 1 本も動きません
（そのビットが立っているレコードもこの 1 本だけ）。

レイヤが動くのは上の行が `②レイヤ変更【有】` だからです。**【無】が
どうなるかは測っていません**。`①指定範囲内変更` と `③属性設定` も同じ。

拾いかたは素の pick——**書込みのペンで絞ったりはしません**（絞ったら
ペンを変える命令として成り立たない）。`jw_cmd_line_at()`、線消 と同じ。
**右ボタンでも同じに動きます。**

拾えなかったときは `.読取可能データ無` を出して**段は進みません**
（`[ESC]` が出ないので分かります）。

```sh
sh tools/pressfull.sh 24 197 157                    # 0 画素
DRAWING=SAMPLE6 sh tools/pressfull.sh 24 446 189    # 円弧、0 画素
DRAWING=SAMPLE6 sh tools/pressfull.sh 24 244 140    # 外し、0 画素
DRAWING=SAMPLE6 sh tools/pressfull.sh 24 499 271    # 2 画素（下記）
DRAWING=SAMPLE6 BOOT=150000000 sh tools/savecheck.sh -c 24 -p 499 271
```

線 38 の 2 画素は両端で、**本物が変えた線をいちばん上に描き直す**ぶん
（交点の色、4.6 と同じ種類）。書き出したレコードは本物のものと
1 バイトも違わず、移植が書いたファイルを本物が開いて 0 画素差です。

**採取の道具もひとつ直しました。** `tools/stage_table.py` は 2 行目の
17 桁と 22 桁を「読取の種類（`src/snap.h`、ポインタについて回る）」として
捨てていましたが、**線消 と 線変更 はそこに自分の言葉を書きます**。
`src/snap.h` を読んで、その命令が実際に使っている桁だけ捨てるように
しました。

### 4.30 複写（1 番の ⑥回転）—— **基準点で回してから置きます**（`src/cmd.c`）

`|①ﾏｳｽ位置|②数値位置|③数値倍率|④ﾏｳｽ倍率|⑤反転|⑥回転|⑦属性変更|` の 6 つめ
（桁 63〜68、x 496〜551）。**①ﾏｳｽ位置 の間に角度が 1 つ入るだけ**です:

| 段 | 何を待つか | 上の行 |
|---|---|---|
| 13 | 基準点 | `複写  原図形の基準点位置 マウス指示 (L)free (R)Read  \|①【任意】方向\|` |
| 14 | 角度 | `[ESC]  ` ＋ 桁 8 の `角度 =`、桁 15 に欄、桁 32 に `｜0 度 ﾏｳｽ(L)｜前回と同じ ﾏｳｽ(R) ｜[F1] ﾏｳｽ角度｜`、帯の桁 50 に `[  45.000゜]` |
| 15 | 置く点 | `[ESC]  複写 位置 マウス指示 (L)free (R)Read  \|①【任意】方向\|②仮表示\|③回転角\|`、帯の桁 71 に打った角度 |
| 16 | （もう 1 つ置ける） | `[ESC] ・ 再複写 位置指示(L)free (R)Read \|①同形別処理\|②他図形処理\|③連続\|` |

13・15・16 の行は `src/copy.h` の 5・6・9 と**一字一句同じ**です
（⑥回転 は ①ﾏｳｽ位置 に角度を挟んだもの）。それでも `src/typed.h` に
別に持たせてあります——`src/ui.c` が回転中は copy.h の 5〜11 を飛ばすのと、
帯が違うからです。

**式は 1 つだけ。補正は一切ありません**:

```
p' = R(θ)(p − 基準点) + 置く点
```

SAMPLE0、範囲 (150,130)-(245,170)、基準点 = 画面 (200,300) ＝ 記録 (79,163)、
置く点 = 画面 (400,300) ＝ 記録 (279,163) で本物に書かせた記録:

| θ | 線 5 (40.973,305.616)-(110.737,305.616) | 文字 0 (51.172,310.957)-(93.030,310.957) |
|---|---|---|
| 3 度 | (233.561,303.431)-(303.230,307.082) | (243.466,309.298)-(285.268,311.489) |
| 30 度 | (174.760,267.496)-(235.177,302.378) | (180.922,277.221)-(217.172,298.150) |
| 150 度 | (240.624,20.477)-(180.207,55.359) | (229.121,20.951)-(192.871,41.881) |

**150 度が要点です。** 回したあとの基線は 150 度——⑤反転 が文字を押し戻す
`-90<θ<=90` の外——なのに、本物はそのままにします。**反転は字の裏表を
ひっくり返すが回転はひっくり返さない**ので、直すものが無いわけです。
端の入れ替えも基線のずれも起きません（4.28 と対）。

**円弧は傾きに入ります。角度 2 つではありません。** TEST1 の範囲
(235,218)-(340,320)、基準点 = 画面 (300,350)、30 度で、弧 0 は
`90..180` のまま、`tilt` が 0 → 30 になります。同じ円ならどちらでも
同じ絵になりますが、**楕円では軸ごと回る**ので違いますし、記録が合いません。
入るのは 16.16 度そのまま——12.5 度で回すと生の値が **819200 ＝ 12.5×65536**
ちょうどで、丸めはありません。（`tools/arcdump.py` は +24 の上位ワードだけを
読んでいて 12 と出していました。いまは +22 から 16.16 で読みます。）

`sh tools/rotatecheck.sh 150 130 245 170 200 300 30 400 300` が画面、
`sh tools/rotatesave.sh 150 130 245 170 200 300 30 400 300` が記録です。

**文字の向こう端は、回した向きに「文字列から出し直し」ます。**
もとの端を回すのではありません。`③連続`（角度が 2 倍になる）で初めて
差が出ました——SAMPLE0 の文字 0 を 60 度回すと本物は
(336.951,212.879)-**(357.881**,249.129)。もとの端を回すやり方は、
丸め方をどう変えても **357.880** にしかなりません。
`jwc_text_length` を新しい向きに置くと 357.881 です。

記録の成り立ちどおりです——文字の広がりは文字列と字種から決まるので、
向きが変われば向こう端は出し直すしかありません。**③数値倍率 は
これをしません**（向きが変わらないので両端がそのまま動く、4.31）。
30 度だけで見ていたら**どちらの流儀でも同じ値に丸まって**、
気づかないまま通っていました。

**`③連続` も入りました。** 2 つめの複写は
`R(nθ)(p − 基準点) + 基準点 + n×(置く点 − 基準点)`——**角度は積み上がり、
倍率は積み上がりません**（4.31 の ③連続 は 1 つめを移動させるだけ）。
どちらも平行移動は n 倍で、①ﾏｳｽ位置 と同じです。
`①同形別処理`／`②他図形処理` もこの段から効きます。

**入れていないもの**: `①【任意】方向`、`②仮表示`、`③回転角`、
角度の `0 度 ﾏｳｽ(L)`・`前回と同じ ﾏｳｽ(R)`・`[F1] ﾏｳｽ角度`（マウスで
2 点指して角度を決めるやり方）。
帯の `[  45.000゜]` は捕ったまま（割付角度で、打った値では動きません）。

**ここでも 1 つ踏みました。** `tools/rotatesave.sh` が最初
`type 3 0` と 1 行で書いていて、エミュレータの `type` は行の残り全部を
取るので**間の空白で欄が終わり、本物は 3 度回していました**。回転として
筋の通った数が返ってくるので気づきにくい種類の間違いです。いまは
1 文字 1 行にしてあります。

### 4.31 複写（1 番の ③数値倍率）—— **基準点で伸び縮みさせて置きます**（`src/cmd.c`）

⑥回転 と**まったく同じ 3 段**（基準点 → 数字 → 置く点）で、数字が角度では
なく倍率です。桁 35〜44（x 272〜351）。

| 段 | 何を待つか | 上の行 |
|---|---|---|
| 17 | 基準点 | ⑥回転 の 13 と同じ（`src/copy.h` の 5） |
| 18 | 倍率 | `[ESC].倍率 X,Y =`、桁 18 に欄、桁 38 に `前回と同じ ﾏｳｽ(R) `、桁 56〜78 に `[     2.000,     2.000]` |
| 19 | 置く点 | `[ESC]  複写 位置 マウス指示 (L)free (R)Read  \|①【任意】方向\|②仮表示\|` |
| 20 | （もう 1 つ置ける） | ⑥回転 の 16 と同じ（`src/copy.h` の 9） |

**段 19 は copy.h の 6 ではありません**——あちらは `\|②仮表示\|③回転角\|`
で終わり、こちらに ③回転角 はありません。

**線と円弧は素直です**:

```
p' = S(p − 基準点) + 置く点
```

SAMPLE0、範囲 (150,130)-(245,170)、基準点 (79,163)、置く点 (279,163)、倍率 2:

| もの | もと | 本物が書いたもの |
|---|---|---|
| 線 5 | (40.973,305.616)-(110.737,305.616) | (202.946,448.232)-(342.475,448.232) |
| 文字 0 | (51.172,310.957)-(93.030,310.957) | (223.344,458.914)-(**265.202**,458.914) |

**文字は始点だけが動きます。** 上の 265.202 − 223.344 ＝ 41.858 は
**もとの基線の長さそのまま**です。これも `JW_CADV.HLP` に書いてあります:

> ※　数値倍率・マウス倍率の場合、文字は指定されている文字の基準
> 　点を倍率複写した位置になります。ただし、角度は変りません。

道理も通っています——字の大きさは図面の表への**番号**なので倍率では
変えられず、基線の長さは文字列と番号から決まる（`jwc_text_length`）ので、
長さも向きもそのままで始点の行き先へ運ぶしかありません。

円弧は TEST1 の弧 0 で測りました（範囲 (235,218)-(340,320)、基準点
(179,113)、倍率 2）: 中心が (165,193.397) → (251,273.795)、半径が
43.6027 → 87.2054、角度・傾き・扁平率はそのまま。

**`JW_CADV.HLP` の順番は違います。** あちらは「基準点 → 複写位置 →
倍率」と書いてありますが、本物は**基準点 → 倍率 → 置く点**です。
文書は読む価値がありますが、順番は測ったほうが正しい。

`ITEM=300 sh tools/rotatecheck.sh 150 130 245 170 200 300 2 400 300` と
`ITEM=300 sh tools/rotatesave.sh …` で比べます（⑥回転 と同じ形なので
同じ道具に `ITEM` を渡すだけです）。

**`③連続` は倍率を掛け直しません。** 2 つめは 1 つめを
`(置く点 − 基準点)` だけ動かしたもので、SAMPLE0 の倍率 2 なら
(202.946,448.232) → (402.946,448.232)。⑥回転 は角度が積み上がるのに、
こちらは積み上がらない——測ったとおりです（4.30）。

**入れていないもの**: `前回と同じ ﾏｳｽ(R)`、`①【任意】方向`、`②仮表示`。
帯の `[     2.000,     2.000]` は捕ったまま（本物が持っている前回値で、
打った値では動きません）。**X と Y を別々にしたとき円弧がどうなるかは
測っていません**——扁平率が変わるはずですが、当てずっぽうを入れるより
X の倍率だけを半径に掛けてあります。

### 4.32 複写（1 番の ④ﾏｳｽ倍率）—— **箱 2 つで倍率を決めます**（`src/cmd.c`）

桁 46〜54（x 360〜431）。打ち込みは無く、**押しが 4 回**です:

| 段 | 何を待つか | 上の行 |
|---|---|---|
| 21 | 基準点 | ⑥回転 の 13 と同じ（`src/copy.h` の 5） |
| 22 | 原図形の対角 | `[ESC]` ＋ `原図形の マウス倍率 対角位置  マウス指示 (L)free (R)Read ` |
| 23 | 置く点 | 4.31 の 19 と一字一句同じ |
| 24 | 複写の対角 | `[ESC]  複写の マウス倍率 対角位置  マウス指示 (L)free (R)Read ` |
| 25 | （もう 1 つ置ける） | ⑥回転 の 16 と同じ（`src/copy.h` の 9） |

**中身は ③数値倍率 です**——倍率が打ち込みでなく、箱 2 つの比になるだけ。
SAMPLE0、範囲 (150,130)-(245,170)、4 回の押しが画面の (200,300) (300,380)
(350,200) (550,360) ＝ 記録の (79,163) (179,83) (229,263) (429,103):

* 原図形の箱は 100 × −80、複写の箱は 200 × −160 → **倍率は 2 と 2**
* 基準点は 1 つめ、置く点は 3 つめ
* 線 5 は (152.946,548.232)-(292.475,548.232)、
  文字 0 は (173.344,558.914)-(215.202,558.914)

全部 `S(p − 基準点) + 置く点` のとおりで、文字は 4.31 と同じく長さを
保ちます（41.858 のまま）。

**道具を 2 つに絞りました。** `tools/reccheck.sh`（画面）と
`tools/recsave.sh`（記録）は **`tests/drawing.exe` と同じ引数**を取って、
本物への台本に翻訳します。片側だけ書き間違える余地が無くなるので、
これから増やす命令はこの 2 つで見ます（`mirrorcheck` / `mirrorsave` /
`rotatecheck` / `rotatesave` は先に書いたもので、そのまま残しています）。

```sh
sh tools/reccheck.sh -c 1 -r 150 130 -p 245 170 -t 580 -t 390    -p 200 300 -p 300 380 -p 350 200 -p 550 360
```

**移動（16 番）の 3 つはまだです。** ⑥回転 の**式は測ってあります**——
本物は同じ座標に、複写せずその場で動かします（SAMPLE0 で線 5・6 と
文字 0 が複写と同じ行き先へ、数は 30|13 のまま）。入れていないのは
上の行で、移動は移動で捕り直しが要るからです（表は命令ごとに捕る決まり）。
いまは 3 つとも `c->command == 1` で塞いであります。

**入れていないもの**: `①【任意】方向`、`②仮表示`。箱の幅か高さが 0 のときは、その軸を 1 倍の
ままにしてあります（**測っていません**——本物は押しを断るかもしれません）。

### 4.33 移動（16 番）の ⑥回転・③数値倍率・④ﾏｳｽ倍率 —— **写さずその場で**

式は複写と同じで、複製を作らず**元のものを動かします**。上の行は
複写のものと同じ形ですが、`複写`→`移動`、`再複写`→`再移動` なので
`src/typed.h` に別に捕ってあります（表は命令ごとに捕る決まり）。段は
複写と同じ 13〜16 / 17〜20 / 21〜25 で、`JW_MOVING()` が分けます。

記録は 3 通りとも本物と一致。画面は **2 画素残ります**が、これは
「本物の消しが隣を巻き込む」既知の口です——(161,157) と (231,139) は
SAMPLE0 の線 0・線 1 の上の点で、動かした線が触れていた所です。
**本物のほうが黒く、移植が白い**（本物が消しすぎている）。

**ここで 1 つ直しました**（`jw_view_mark`）。`jw_line` は切り取りを
しません——`v->clip_*` は呼ぶ側の仕事で、`inside` の注記のとおりです。
範囲の赤い印（`jw_cmd_marked`）はそれを知らずに画面座標を渡していて、
**複写だけが使っているうちは出ませんでした**（複写は選んだものを動かさない
ので、いつも窓の中にある）。移動は選択ごと動くので、倍率 2 で
SAMPLE0 の線 5 が画面の y=14.77 に行き、**赤い 141 画素が上の行に**
乗りました。`jw_view_mark()` で作図の線と同じ切り方をさせて 157→2 画素。

**まだないもの**: 移動の ③連続（複写は入りました）。移動はもう元のものを
動かしてしまっているので、次の一歩が何を動かすのかを**測っていません**。
当てずっぽうを入れるより、押しても何もしないようにしてあります。

### 4.45 変形（17）①パラメトリック変形 —— 範囲の中の端点だけが動く

**道は 複写 と同じでした。** 本物の行は `src/henkei.h`（`tools/henkei_table.py`
で撮ります）。範囲 2 点 → 追加･除外 → ①範囲 確定 →
`|①ﾏｳｽ位置(L,R)|②数値位置|③数値倍率|`。

**作図領域を押すと ①ﾏｳｽ位置 を選ぶのと基準点を取るのが同時**です。上の行の
セルを押したときだけ 段 5（`変形  原図形の基準点位置`）で止まります。

| 道 | 段 | 式 |
|---|---|---|
| ①ﾏｳｽ位置 | 5 → 6 → 9 | 置く点 − 基準点 |
| ②数値位置 | 7 → 8 | 打った `X,Y` × unit_mm（`20,30` で (34.883,52.323)） |
| ③数値倍率 | 5 → 18 → 19 → 20 | **S(p − 基準点) + 置く点**（複写 の ③数値倍率 と同じ式） |

**動くのは範囲の中にある端点だけ**で、外の端点はそのままです。SAMPLE0・
範囲 (200,150)-(450,350)・(+50,-50) で

```
line 5 (40.973,305.616)-(110.737,305.616)
    -> (40.973,305.616)-(160.737,255.616) 01 02 00 41 02 02
line 6 (110.737,305.616)-(110.737,323.057)
    -> (160.737,255.616)-(110.737,323.057) 01 02 00 41 02 01
```

最後のバイトは**どちらの端を引っぱったか**——始点だけなら 1、終点だけなら
2、丸ごと動けば 0。その前のバイトの bit 1 は触ったもの全部に立ちます。

**どの端を引くかは一度決めたら測り直しません。** 再変形で範囲の外へ出た端も
引き続き動きますし、赤い印もその控えから出します（②数値位置 の `20,30` は
line 5 の端を範囲の外へ運びますが、本物はその線を赤いままにします。箱で
測り直すと 141 画素違いました）。

画面は 3 つ:

* **伸びるものは赤の点線（1 画素おき）、丸ごと動くものは赤の実線**。
  複写 は丸ごと入っているものしか取りません
* 左ボタンの範囲は**文字を取りません**（`takes_text` に 17 を入れること）
* 位置を聞いている間は**色 4 の排他的論理和**で、伸ばした（倍率なら
  拡げた）形がポインタについてきます

#### 4.45b 変形 ③複線化 —— **輪郭を作る**（入りました）

`|③複線化|` は桁 51〜58（x 400〜471）。範囲の道は ①パラメトリック変形 と
同じものを通り、言葉だけ違います（段 3 は 文字(R) を取らないので
`線・円(L)` だけ）。①範囲確定 のあとが

```
複線化 |① 実行(L)|② 中止(R)|③間隔  100.00(mm)|④留線【有】|(円不可)
```

で、`① 実行` は桁 16〜25、`② 中止` は 27〜36、`③間隔` は 38〜55、
`④留線` は 57〜68。②中止 も ①実行 のあとも 段 0 に戻ります。

**入るのは、範囲に丸ごと入っている線をつないだ折れ線の「輪郭」** です。
SAMPLE0 の線 5（(40.973,305.616)-(110.737,305.616)）だけを 100mm
（= 174.411 単位）で実行すると

```
line (-133.438,480.027)-(285.148,480.027)   左側
line (-133.438,131.205)-(285.148,131.205)   右側
line (-133.438,480.027)-(-133.438,131.205)  始めの留線
line ( 285.148,480.027)-( 285.148,131.205)  終わりの留線
```

—— 元の線を四方に 174.411 ふくらませた長方形です。線 5 と線 6 の L 字だと
六本になり、角では両側とも **交わるところ** で折れます。並びは「一本目の
左・右・始めの留線、二本目の左・右、…、終わりの留線」。記録は元の線の
線種・ペン・レイヤ・A バイトをそのまま継ぎます。

* **④留線【無】** にすると留線が消え、**両端の伸ばしもなくなります**
  （線 5 だけなら元と同じ長さの二本だけ）。行からも `(円不可)` が消えます。
* **③間隔** は `[ESC].間隔 =`（`.` は本物の句点で、BEL ではありません
  ——画面に 2×2 の白が出ます）と、桁 38 の `前回と同じ ﾏｳｽ(R) `、
  桁 56 の `[%10.3f`、桁 67 の `mm`、桁 69 の `]`。打ち込みは桁 14 から 8 つ。
* **半分だけ入っている線は赤くなりません。** ①パラメトリック変形 は
  伸ばす線を赤の点線にしますが、③複線化 は丸ごと入っている線だけです。
* **入れた線は範囲を放したあとも枠の上です**（測定：留線が帯の下の 2 行
  ——項目を選ぶと黒くなるところ——にも出ていました）。`jw_cmd_after` を
  段 0 に戻ったあとも通します。
* つながらない線が混じったときに本物が何をするかは測っていません。
  円は取れません（行に `(円不可)` とあります）。

#### 変形の残り 3 つ（測っただけ）

| 項目 | 本物が出す行 | 分かっていること |
|---|---|---|
| ②包絡処理変形 | `包絡範囲  始点指示 |①【実線のみ】|(同一線色・線種を包絡)` | 包絡（同一レイヤ・線色・線種）の範囲処理。未着手 |
| ③複線化 | 4.45b に**入りました** |
| ④線記号変形 | `マウス指示 |①種類【A】変更|` | 記号のファイルは `JW_OPT4.DAT`〜`JW_OPT4J.DAT`。**書式はその DAT の 133 行目以降に平文で書いてあります**（下） |
| ⑤外部 | `[ESC] ファイル選択 |①選択確定 |②ドライブ(A:)変更 |③子プロセス |④拡張子変更 |` | 外部プログラムを起動する道。⑦外部処理 と同じで**やらない**（利用者の指示） |

#### 4.45e 変形 ④線記号変形 の記号ファイル —— **書式は同梱されています**

`orig/JW_OPT4.DAT` の 133 行目から、**この書式の説明がそのまま日本語で
入っています**（`iconv -f CP932` で読めます）。`JW_CAD.DOC` の
「（６）記号変形用データ」が「記述方法は［JW_OPT4.DAT］をご覧ください」と
指しているのがそれです。使うファイルは `JW_OPT4.DAT`・`JW_OPT4B`〜`J`。

読み取れる骨は——**まだ一つも測っていません。下は DAT の文面の写しです**:

* 頭が `#名前` でグループ名、次の行がデータ数、`999` が区切り
* データの先頭行は `指示回数<空白>データ名`。0 なら 1 回指示で描き始め、
  10 なら何も指示しない
* 線は `制御(1) 制御(2) 始点X 始点Y 終点X 終点Y`。制御は
  `10`=指示線1、`20`=指示線2、`01`/`02`=その角度に追従、`03`=指示線2の
  半分、`04`=二本からの距離、`00`=影響されない、`08`=表だけ、`09`=ダミー
* 制御(1) が 3 桁で 100 の位が 1／2 なら、線色・線種・レイヤを指示線 1／2
  と同じにする（`101`・`202`）
* 円は `制御(1) 制御(2) 中心X 中心Y 始角 終角 線色 線種 レイヤ e 半径`。
  半径が負なら楕円で、続く数が偏平率
* 文字は線色に `10000`、線種が文字種。`"` のあとが文字列。文字種を 3 桁に
  すると基点（1 中下〜8 右上）、4 桁で 1000 の位が 1 なら縦書き
* 実点は線色に `30000`
* `700 倍率` は次の行から図面寸法の倍率、`800 倍率` は実寸法。倍率が負なら
  180 度回した向き
* `10000 番号` で書いたあと次の記号へ自動で移る。`10000 10000` で選択画面へ
* 制御(1) が `10100`（複写）・`11300`（文字入力）・`11400`（寸法）…なら、
  書いたあとそのコマンドへ一時的に移る。`15000` は文字の左ドラッグ指定
* 部材は 1 データ 60 個まで。文字入力・変更・スキップの指定は 50 個・
  合計 200 バイトまで

**約束どおり、実際に入れるときはこの文面を鵜呑みにせず、本物に描かせて
測り直します。**

#### 4.45c 変形 ②包絡処理変形 —— 入口・範囲・範囲内消去（入りました）

`|②包絡処理変形(R)|` は桁 33〜49（x 256〜391）。押すと自分の行になります:

```
包絡範囲  　始点指示 |①【実線のみ】|(同一線色・線種を包絡)      [BS]前項
```

`①【実線のみ】` は桁 30〜43（x 232〜351）で、押すと `①【全 線 種】`。
始点を取ると（どちらのボタンでも free）

```
[ESC]  包絡範囲  　終点指示 ﾏｳｽ(L)   範囲内消去 ﾏｳｽ(R)
```

になり、赤い枠が矢について回ります（`JW_RANGE_CMD` の枠と同じ）。
**終点を左で押すと包絡、右で押すと範囲内消去**。どちらのあとも
始点を待つ行に戻り、一度でも何かしたあとは桁 1 に `[ESC]` が付きます。

**枠にかかる線は五十本まで**です。越えると行 2 の桁 20 に
**BEL つきの** `線数は５０までです` が出て、**何もしません**
（測定：五十本ちょうどは切れて百本になり、五十一本はそのまま）。
数えるのは枠にかかった線ぜんぶ——①【実線のみ】で切らない線種も込みです。

**範囲内消去** は切り取りです。丸ごと入っている線は消え、またいでいる線は
枠の線ちょうどで切られ、**外に出ているぶんだけ**が新しい記録として後ろに
並びます（線ごとに手前側・向こう側の順）。線種・ペン・レイヤと残りの
バイトは元のものを継ぎます。**①【実線のみ】のときは実線（線種 1）しか
切りません**——SAMPLE1 の線種 9 は選ばれるのに残ります。
`rest[2]` の bit 1（触った印）は**まず全部落としてから**、枠にかかった
ものぜんぶに立ちます。

`DRAWING=SAMPLE0 sh tools/seqcheck.sh "30 88 left" "320 8 left" "200 150 left" "450 350 right"`
—— SAMPLE0・SAMPLE1・SAMPLE2 と `tools/mkhoraku.py` の図面で 0 画素差。

**合っていないもの**（次にやるならここから）:

* **選ばれたのに切られなかった線を、原作は画面から消したままにします。**
  SAMPLE1 の線種 9（28 画素）、TEST7（1625 画素）。原作は選んだものを
  黒で塗ってから、残ったぶんだけ描き直しているようです。移植は記録から
  描き直すので残ります。
* SAMPLE0 の枠ちょうど (160,140)-(600,420) で 3 画素——切って残った
  **画素より短い切れ端**（161.973 の y 139.943〜140.000）を原作は
  塗り直しません。開き直すと両方いっしょです。
* TEST1・SAMPLE3・TEST7 は**レイヤが複数**あるところで記録から違います。

#### 4.45d 包絡そのもの（外形線・線連結まで入りました）

同梱の `JW_CAD.DOC` が数えている通り**場合分け**です——「Ｌ型、Ｔ型、
＋型、柱／壁、外形線、線連結、範囲内消去、その他」。測るのは
`tools/mkhoraku.py` の図面（`python tools/mkhoraku.py orig/SAMPLE0.JWC
orig/TEST8.JWC walls` で作り、**測り終えたら消すこと**——`tools/check.sh`
は orig/ の図面を十四枚と数えています）。

**触れ合うのは同じレイヤ・同じ線色・同じ線種どうしだけ**です。壁の片方
だけペンを変えると何も起きませんでした。①【実線のみ】のときは線種 1 しか
参加しません（選ばれはします）。**枠にかかる線は五十本まで**。

手順は四つ:

1. **線連結** —— 一直線に並んでいる仲間を先に一本につなぎます。離れた
   二本でも、内側の端が枠の中にあればつながります（端が枠の縁ちょうど
   だと、その線は選ばれないのでつながりません）。
2. **壁があるか** —— 平行な二本が一組でもあるか。一組も無いまま全部が
   枠に丸ごと入っているときは、**交点で切られるだけ**です（一本ずつの
   十字は四本に、Ｔ字——相手の端点で触れているだけ——は切れません）。
3. **枠に丸ごと入っている線（外形線・柱）** —— 直角の向きの仲間との
   交わりの**端から端まで**に伸び縮みします（相手も無限に伸ばして
   交わりを取ります）。交わる相手が一本しかないときはそのまま。
   **真ん中の線**（仲間の中でいちばん端でないもの）は、**両方の向きに
   真ん中の線があるときだけ**消えます——格子・入れ子は外の四本だけに
   なり、棚（真ん中の横線だけ）や段（真ん中の縦線が無い）はそのまま。
4. **枠をまたいでいる線** —— 交点で切って、**両側とも壁の中身に覆われて
   いる区間**を落とします。ただし覆っている壁の少なくとも一方は自分と
   平行でないこと。**壁の中身**というのは、平行な二本にはさまれ、しかも
   二本とも横に伸びているところ——端で触れているだけは中身ではありません。

これで `tools/mkhoraku.py` の 21 通りが画面 0 画素差:

| 図 | 枠 | 出てくるもの |
|---|---|---|
| `walls` 壁が十字 | またぐ／丸ごと | ＋型／20×20 の四角 |
| `twall` Ｔ字の壁 | またぐ | 通し線はそのまま、突き当たる側だけ切れる |
| `lwall` Ｌ字（行き過ぎ） | またぐ | 外側は残り、内側だけ切れる |
| `lcorner` Ｌ字（角合わせ） | またぐ | 変わらない |
| `diag` 斜めの壁 | 両方 | 斜めでも同じ |
| `farwall` 離れた平行二本 | 両方 | 同じ |
| `onev`/`oneh` 一本が壁を横切る | 両方 | 壁を持たない側は動かない |
| `threewall` 縦の壁が二つ | 丸ごと | 横線は端から端まで、縦線は帯の中だけ |
| `one` 長方形 | 丸ごと | 変わらない |
| `cross`／`tee` 一本ずつ | 丸ごと | 交点で切るだけ／変わらない |
| `elltrim` 一本ずつの十字 | またぐ | 変わらない |
| `shelf` 長方形＋真ん中の横線 | 丸ごと | 変わらない |
| `cell` 格子（田の字） | 丸ごと | 外の四本だけ |
| `nest` 入れ子の長方形 | 丸ごと | 外の四本だけ |
| `two`／`ell` 離れた／重なる長方形 | 丸ごと | 外接長方形 |
| `twosep`／`stack` 並んだ／積んだ長方形 | 丸ごと | 全部残り、一直線のものはつながる |
| `colin` 一直線の二本 | またぐ | 一本につながる |
| `ladder` 桁と、そこで終わる横棒 | またぐ | 変わらない（7 画素差） |

本物の図面では SAMPLE0（二通り）・SAMPLE2・SAMPLE6・TEST1 が 0 画素差。

**まだ合っていないもの**:

* **枠が一部の線だけを丸ごと覆っているとき**（`wallmix`、259 画素）。
  原作は、丸ごと入っている線を先に処理しているように見えます——出てくる
  記録の順が入力の順と違い、またいでいる線からも**壁より向こうが丸ごと**
  消えていました。規則がまだ読めていません。
* SAMPLE1（109）・SAMPLE3（127）・SAMPLE0 の枠いっぱい（477）。
* **原作の「線数」箱は古い数のまま残ることがあります。**
* 選ばれたのに切られなかった線を、原作は画面から消したままにします
  （4.45c）。

### 4.35 メニューは指しただけで反転します

`tools/hovercheck.sh`——**押さずにポインタを置くだけ**で本物と突き合わせる
検査を足しました。既存の検査は全部「まず何かを押す」ので、
**置いただけの状態は 1 度も比べていませんでした**。

いきなり 2 つ出ました:

* メニューの升は**指した時点で反転**します（白地に黒字）。移植は押した
  ときだけでした。升は左が x 8〜55、右が 72〜111、行は y=64 から 16 画素
  ごと。**字のところだけ**で、横の一文字キーは反転しません
* 数え箱（線数・円文数の黄緑）にポインタが入ると、箱が黒くなって
  **目盛／軸角の小さな盤**が出ます。**作りました**（4.38）

### 4.36b 縮尺の欄は「倍率」でした

`倍率=` に `2` を入れると、本物の左の盤は **`S=2.0/1`** になります——
`S=1/2.0` ではありません。つまり欄は**倍率**で、denom はその逆数です。
図面は**原点中心に 2 倍**（画面の箱が x 161〜598・y 139〜419 から
x 201〜・〜y 375 になり、右と上ははみ出して切れます）。紙と同じ規則の
裏返しで、係数は「前の denom ÷ 新しい denom」です。

`WHAT=scale N=2 sh tools/papercheck.sh` で **103 画素**まで来ました。
残るのは**文字の箱 1 つの幅**だけです（本物 x 237〜244 の 8 画素、移植
x 230〜258 の 29 画素、高さはどちらも 6 行で同じ）。変える前は
x 168〜189 の 22 画素なので、本物は 22 → 8、移植は 22 → 29。ほかの文字
は合っているので、この 1 つだけ別の決まりで出ています。**未解明**です。

### 4.28 複写（1 番の ⑤反転）—— **範囲を線で折り返して写します**（`src/cmd.c`）

`|①ﾏｳｽ位置|②数値位置|③方向･･|④倍率|⑤反転|⑥回転|⑦線種変更|…` の 5 つめ。
範囲を決めたあとで ⑤反転 を押すと、上の行が

* `[ESC]` ＋ 桁 8 の `反転基準線　マウス指示 `（段 10）

になり、**線を 1 本押す**とその線を鏡にして範囲を写します。写し終わると

* `[ESC]` ＋ 桁 6 の `・` ＋ 桁 8 の `|①同形別処理|②他図形処理|`（段 12）

です。段 10 と 12 は `src/copy.h` が使っていない番号で、`src/typed.h` に足しました。
**複写の表の段 5〜11（①ﾏｳｽ位置 の置き方）は反転の間は出ません**ので、
`src/ui.c` の並べ直しで `s->mirror` のときだけ飛ばします。

**端の順番が線と文字で違います**（SAMPLE0、範囲 (150,130)-(245,170)、
鏡は線 0 ＝ x=40.973 の縦線）:

| もの | もとの記録 | 本物が書いた記録 |
|---|---|---|
| 線 5 | (40.973,305.616)-(110.737,305.616) | (40.973,305.616)-(**−28.791**,305.616) |
| 線 6 | (110.737,305.616)-(110.737,323.057) | (−28.791,305.616)-(−28.791,323.057) |
| 文字 0 | (51.172,310.957)-(93.030,310.957) | (**−11.084**,310.957)-(**30.774**,310.957) |

線は端の順をそのまま持ちます。**文字は場合によって入れ替わります**。
規則は同梱の `JW_CADV.HLP` に平文で書いてありました（複写 5)反転）:

> 文字方向を、横字は-90<θ<=90、縦字は-90<=θ<90 の方向に修正します。

つまり折り返したあとの基線の角度をその半回転に入れ直す、ということで、
180 度動かす手は端を入れ替えることしかありません。`tools/mirrorsave.sh`
（本物に反転させて保存させ、増えた記録を移植のものと並べる）で 3 回測って、
**どれも「横字」の範囲だけで説明が付きます**:

| 図面 | 文字 | 鏡の角度 | 折り返し後 | 端 |
|---|---|---|---|---|
| SAMPLE0 | 文字 0（0 度） | +90 | 180 → 範囲外 | **入れ替わる** |
| TEST1 | 文字 10（0 度） | −38.05 | −76.1 → 範囲内 | そのまま |
| TEST1 | 文字 7（**+90 度**、上向き） | −90 | +90 → 範囲内 | そのまま |

3 つめが効きます。+90 は「横字」の範囲に入り「縦字」の範囲からは外れるので、
**基線が上を向いているだけでは縦字ではありません**——縦字は JW_CAD の
縦書き文字種のことで、移植はそれを作りません。

**入れ替わらないときは基線が 1 文字ぶん横にずれます。** 記録が持つのは基線
だけで、字はいつもその片側——左から右に走る字なら上——に描かれます。
折り返すと絵の側が裏返るので、端を入れ替えればもう一度裏返って何も動かさずに
済みますが、入れ替えないときは**基線のほうが字をまたいで向こう側へ動く**
必要があります。動く量は**文字の高さ** `text_h / 10 × unit_mm`——幅では
ありません:

| 図面 | 文字 | 高さ | 動き |
|---|---|---|---|
| TEST1 | 文字 10（字種 2、unit_mm 0.872054） | 2.180 | (−2.106,−0.527) ＝ 2.180 × (dy,−dx)/len |
| TEST1 | 文字 7（字種 10） | 8.721 | x が **+8.721**、y は動かない |
| SAMPLE0 | 文字 0（字種 3、unit_mm 1.744108） | 5.232 | 線 4（水平）で折り返して y が −188.075 → **−193.307** |

3 つめは**走らせる前に −193.307 と書いてから測りました**。本物も −193.307
です。円弧は**測っていません**（SAMPLE0 にも TEST1 にも範囲に入る弧が
なかった）。中心を折り返して 2 つの角を折り返して入れ替える、という
幾何どおりにしてあり、これは当てずっぽうです。

**レイヤ 1 のものは範囲に入りません。** SAMPLE0 の縦書き文字（文字 2・9・10）
も寸法線も `lay=01` で、範囲に囲っても複写されません。縦書きを測るのに
TEST1 まで行ったのはそのためです。

**比べ方は 2 つあります。**

* `sh tools/mirrorcheck.sh 150 130 245 170 162 279` と
  `TEXT=1 sh tools/mirrorcheck.sh 160 140 230 165 162 279`（範囲の 1 点目を
  右押しにして文字も入れる）が**画面**。どちらも 0 画素差
* `sh tools/mirrorsave.sh 150 130 245 170 300 402` が**記録**——本物に
  反転させてから保存させ、増えた記録を移植のものと並べます。文字の端の
  順も基線のずれも画面には出ないことがある（窓の外に飛ぶ）ので、
  こちらが要ります。1 件 2 分

`tools/whatsnew.py` は 2 つの図面を比べて増えた線・弧・文字だけを出します。
`tools/linedump.py` は `tools/textdump.py` の線版、`tools/stagestr.py` は
`src/*.h` の段の表を読める字にして出します。

**ここで 1 つ罠を踏みました**（`draw_text_box`）。折り返した文字
`Ｈ７－Ａ００１` は x=−11.084 から始まる——画面では桁 109 で、作図ウインドー
（桁 122 から）の**外**です。読めない大きさの文字は箱で描くので、移植は
`inside()` が偽になった時点で**箱ごと捨てて**いました。本物は違います:

* 本物は箱の横 2 本を**桁 122 から**引き、桁 109 の縦 1 本だけを捨てる

`FUN_1def_17bb` に「点か線か」の引数があって、**点のときだけ**
DGROUP `0xb5aa`/`0xb5b2`/`0xb60e`/`0xb612` の 4 つと比べて外なら戻ります
（`inside()` はこれを写したもの）。**線のときは逆**で、両端が中なら
`FUN_1def_0000` にそのまま渡し、両端が同じ側の外なら捨て、**またがるものは
その 4 つで切ってから**引きます。箱の 5 本もこれを通るので、
`box_line()` を作って `jw_view_line` と同じ切り方をさせました。捨てていた
分がちょうど 75 画素です。`draw_text_box_turned`（斜めの文字の箱）は
またぐ例をまだ測っていないので、当てずっぽうを足さず捨てたままにしてあります。

**まだないもの**: ⑥回転、⑦線種変更 以降、`①同形別処理`／`②他図形処理`。
